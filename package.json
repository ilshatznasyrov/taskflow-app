{
  "name": "taskflow-app",
  "version": "1.0.0",
  "description": "TaskFlow - учебное приложение для автотестов",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}